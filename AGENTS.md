# AI編集規約

このリポジトリは、数学・物理学・化学の公開学習ノートです。以下はリポジトリ全体に適用します。

## 編集の前に

- `README.md`、`docs/writing.md`、`docs/importing.md` と対象分野の入口を読む。
- 関連ノートを検索し、重複作成を避ける。既存の用語・記号を確認する。
- 学びの整理を目的とし、ユーザーの問い、疑問、思考の経緯を保つ。

## 内容

- 日本語で簡潔に書き、必要に応じて英語の専門用語を添える。
- 定義、仮定、導出、直感、推測を区別する。適用条件を省略しない。
- 出典、引用、数値、ページ番号を捏造しない。未読の資料を確認済みの根拠にしない。
- 検証できない内容は未確認と明記する。AIの補足を原文由来の理解として扱わない。
- 数学は論理と前提、物理は次元と近似、化学は条件と収支を確認する。
- 原文の誤りを訂正した場合は、何をなぜ変えたかを作業報告に残す。
- 全文移植ノートの「当時の本文」は、疑問・感想・未完の箇所も含めて保持する。訂正や新しい理解は「AIによる補足・訂正」など別の節に記す。
- 過去の意図は本文の根拠へリンクする。資料の収集を読了、更新日を学習日と推測しない。
- 内容と出典を人が確認した指示がない限り `status: reviewed` にしない。

## ファイル

- 本文はMarkdown、ファイル名は英語の `kebab-case`、リンクは通常の相対リンクを使う。
- 一つの問い・概念につき一つの主なノートとし、複数分野では相互参照する。
- 新規ノートにはテンプレートのメタデータを付け、空の節や案内文を除く。
- ノート追加時は分野の入口を更新し、移動・改名時は参照元のリンクを直す。
- 原文は `inbox/` に保持する。私的な原文や添付を一括で公開用フォルダへコピーしない。
- 今回明示的に全文移植を依頼された公開数学メモは `math/sources/` にも本文を保存している。以後の追加でも、非公開素材をこの扱いへ自動的に広げない。
- 作業用の公開候補は `inbox/drafts/` に置ける。公開用の配置は依頼された範囲に従う。
- ファイルやフォルダは内容が増えたときに追加し、空の分類を大量に作らない。

## 作業の終わりに

- リンク、数式、出典の対応、公開対象への私的情報の混入を確認する。
- 数学の移植を変更したら `python3 scripts/import_scrapbox_math.py --check` で原文・本文・リンクを照合する。生成スクリプトで本人の追記を上書きしない。
- 変更点、確認した内容、未確認の点を短く報告する。
